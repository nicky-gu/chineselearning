name = "hanzi-learning-api"
main = "worker.js"
compatibility_date = "2024-01-01"

# 环境变量（在 Cloudflare Dashboard 设置后生效）
# Variables: HANZI_GITHUB_TOKEN

# 路由配置（部署后需要设置）
# routes = [
#   { pattern = "api.yourdomain.com/*", zone_name = "yourdomain.com" }
# ]
